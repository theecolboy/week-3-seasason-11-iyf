# Tests

This folder is reserved for future tests and validation notes.

Current manual checks:

- Open `index.html` in a browser.
- Test the navigation links.
- Resize the page to confirm the mobile menu appears.
- Check that the contact form labels match their fields.
